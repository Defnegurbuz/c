#include <stdio.h>

int main() {
    int x,i=1;
    printf("Input number of lines: ");
    scanf("%d",&x);
    int n =3*x;
    for(i;i<=n;i++)
    if(i%3==0)
    printf("%d \n",i);
    else
    printf("%d ",i);

    return 0;
}
